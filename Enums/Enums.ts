enum setData {
  a = "sky",
  b = 1,
  c,
  d,
}

const data = setData.a;

// ✅ TypeScript enum definition:
// An enum (short for enumeration) in TypeScript is a way to define a set of named constants,
// making your code more readable and maintainable.

export {};
